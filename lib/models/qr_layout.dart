/// Visual layout variant for a VCard QR entry.
enum QrLayout {
  /// Minimal layout: clean, low-ink design with essential fields only.
  minimal,

  /// Classic layout: traditional card-style presentation.
  classic,

  /// Modern layout: bold, full-bleed contemporary design.
  modern,
}
