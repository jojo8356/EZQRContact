{
  "name": "ezqrcontact-tooling",
  "private": true,
  "description": "Git hooks and commit-message linting for the EZQRContact Flutter repo. No application code is shipped from Node — this package.json only carries devDependencies for husky + commitlint.",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.5.0",
    "@commitlint/config-conventional": "^19.5.0",
    "husky": "^9.1.6"
  },
  "packageManager": "pnpm@10.27.0"
}
